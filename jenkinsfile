pipeline {
        agent {label 'aws-linux'}
        stages {
            stage ('Hello') {
                steps {
                    sh 'echo "Hello World"'
                }
            }
        }
}
