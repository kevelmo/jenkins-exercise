pipeline {
        agent {label 'aws-linux'}
        stages {
            stage ('Hello World') {
                steps {
                    sh 'echo "Hello World"'
                }
            }
        }
}
